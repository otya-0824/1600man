# リリース準備メモ（もぐバランス）

ストア公開に向けた残タスク。チームで方針を決めてから着手する。
（2026-09-19 時点。アプリ機能・アイコン・アプリ名・Firebaseセキュリティは対応済み）

---

## ✅ 対応済み
- アプリ名「もぐバランス」／アイコン（サラダボウル）全プラットフォーム設定
- Firestore：本人のuidのみ読み書き可（`auth.uid == userId`）＋匿名認証（ログイン画面なし）
- 秘密情報の混入なし。署名鍵を `.gitignore` に予防登録
- プロフィール保存前の入力値検証（`UserProfile.validate()`）
- Bundle/Application ID：`com.otya0824.man1600`（実ドメイン形式でOK）
- バージョン：`1.0.0+1`

---

## 🔴 公開に必須（未対応）

### 1. Android リリース署名
- 現状：`android/app/build.gradle.kts` で release がデバッグ鍵署名 → **Google Play は拒否**
- やること：
  1. リリース用キーストア（`.jks`）を作成（`keytool`、パスワード設定）※**一度きり・紛失/流出厳禁**
  2. `key.properties` を作成し `build.gradle.kts` に署名設定を追加
  3. `flutter build appbundle` で `.aab` を生成して提出
- 注意：鍵はチームで安全に共有・保管する場所を決める（紛失すると以後アップデート不可）

### 2. プライバシーポリシー
- 理由：プロフィール（身長/体重/年齢/性別/目標）を Firebase に保存するため両ストアで必須
- やること：
  1. 文面作成（収集項目・目的＝栄養計算・保存先＝Firebase・削除方法＝アプリ内初期化・問い合わせ先）※下書きはClaudeが作成可
  2. Webで公開しURL化（GitHub Pages / Googleサイト / Notion公開ページ 等・無料可）
  3. URLをストア申請フォーム＋アプリ内（設定画面）に掲載

### 3. iOS 提出まわり（iOSを出す場合）
- Apple Developer Program（年額 約$99）契約
- Xcode で署名チーム設定・証明書（多くは自動）
- `Info.plist` に暗号化申告 `ITSAppUsesNonExemptEncryption = false`（標準HTTPSのみ）を追加

---

## 🟡 推奨（審査リスク低減・品質）
- **データ削除導線の強化**：現「ログアウト/データ初期化」はローカル削除＋匿名サインアウトのみ。
  Firestoreの自分のドキュメント削除（`doc.delete()` + `currentUser.delete()`）まで行うとApple要件に強い
- **健康アドバイスの免責表示**：カロリー/栄養目標を出すため「医療助言ではない」旨をUI（FAQ/設定）に明示
- **ストアの追加フォーム**：App Store プライバシーラベル / Google データセーフティ（収集データの申告）
- **将来のログイン**：機種変更・複数端末でデータ引き継ぐならメール/Googleログイン（匿名アカウントを昇格）
- 残 deprecation（DropdownButtonFormField `value:`→`initialValue:`）※値反映の退行に注意しつつ対応可

---

## メモ
- Firebaseプロジェクト：`hakkason-b9738`（Sparkプラン）
- 決め事：署名鍵の保管場所／プライバシーポリシーの公開先／iOSを出すか（Apple Developer契約者）
